# uc_ledger_AI PATCH — paste into the MLB repo ledger

Append row:

| 42 | uc-pos-016 | Trea Turner whole-field directional tendency, 2023–2026, pitch-level BIP sliced by `game_year × p_throws × hit_direction × in/outside-zone`. **Premise tested, not confirmed**: client's working theory — that Turner is pulling outside-zone pitches too often in 2026 — is **NOT supported**; 2026 outside-zone pull rate 39.4% (n=71) is statistically indistinguishable from the pooled 2023-25 rate 36.7% (n=228), \|z\| < 1.5. **What the data shows instead**: on pitches **inside the zone**, pull rate has declined and oppo rate has risen 2023→2026, clearing \|z\| > 2.5 on the same pooled two-proportion test — a real, measurable shift, just not the one hypothesized and not on the pitches hypothesized. Reported as a split verdict rather than smoothed into confirm/deny. Five client-supplied corrections carried in verbatim (repo-sourced data not local CSV export, governed `hit_direction` not hardcoded, locked brand-consistent `p_throws` color map, fixed baseball-diameter marker size replacing `release_speed` sizing, MLBAM-id entity lock not name match). NEW **`sort_rank(direction, stand)`** — client described this as an existing function with a known handedness bug; Rule-1 search found no such function anywhere in either repo, so a new stand-aware version was built and shipped **provisional/unratified** rather than fabricating a "fix" to unseen prior logic. NEW **WF-1** (oppo_rate_ooz) / **WF-2** (pull_rate_ooz) — diagnostic-only, not ledgered as formal KPIs pending a second independent reuse. Two referenced attachments (`tt_whole_field_uc.md`, a hand-sketch photo) never reached the session despite being described as attached; built from the client's prompt text alone, logged as a non-blocking gap, escalated for his confirmation | Delivered 2026-09-10 · **33/33 independent spot-verification PASS** · DQ **22 PASS / 1 WARN (comparability — outside-zone BIP volume/power differs materially by year) / 0 FAIL** · Certification **READY-CONDITIONAL** (C1 = outside-zone test underpowered at n=71, stated not hidden; C2 = `sort_rank`/WF-1/WF-2 unratified pending second reuse; C3 = not yet reconciled against the client's own draft/sketch, neither reached this session) | `Agents for Data Products/data-products/uc-pos-016-turner-whole-field-directional-001/` — `dp_uc42_*` (kernel, build, verification, pdf-builder, dashboard-builder, 5 CSV receipts + 2 figures, 00–07 + README + BID + this patch). Fourth competitive-bid UC: bid ~145k/69k/1h35m ≈ $4.90 → actual ~172k/65k/**~2h10m** ≈ **$5.85**, over on input tokens only (see below), on/under budget on output tokens and every other phase. **Calibration finding, new to this repo's bid history**: this was the first build in the Agents-repo/MLB-repo project pairing to hit a broken device bridge (`device_bash` virtiofs mount failure, never recovered mid-session) and a fully egress-locked cloud sandbox (PyPI, npm, apt, cdnjs all `403 host_not_allowed`) simultaneously — environmental risk on a cold pairing was underpriced ~2x on input tokens because every prior calibration note in this repo reconciled a *warm* environment against instrumented history, not a cold one hitting infrastructure failure. Recommendation for the next bid in this pairing: price a "cold intake + environment verification" phase explicitly, separate from analytical work. NEW build-time-only tool **`pq_reader.py`** — pure-Python Parquet reader (Thrift compact-protocol footer parsing, hand-rolled raw-Snappy decompression, RLE/dictionary page decoding) built because no `pyarrow` was reachable in the cloud sandbox; validated via coordinate-convention assertion (median `loc_x` < 0 for Pull BIP) and the 33-check harness; explicitly **not** part of the shipped kernel and not a reusable asset — the shipped `dp_uc42_kernel.py`/`dp_uc42_turner_whole_field.py` use the standard `pandas.read_parquet` path for the real data plane. Also substituted `reportlab` for the house `weasyprint` PDF path (network-unavailable) and reused `uc-pos-014`'s vendored `_chartjs_4.4.1.umd.js` inline (not re-fetched) for the dashboard, rather than depending on a live CDN — continuing the vendor-don't-CDN dashboard convention established at `uc-pos-011` |

Update "Next available": **UC #43 / dp_uc43** (pps next **uc-pps-029** · pos next **uc-pos-017**).
